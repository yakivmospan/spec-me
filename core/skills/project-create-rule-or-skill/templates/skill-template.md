---
name: {{kebab-case, the same as the folder}}
description: Use when asked to "{{trigger in the user's words}}", "{{another}}", or {{the situation it must run in}} — {{what it does, one clause}}. Not for {{a near-miss request}} ({{what to use instead — a skill of core or of this skill's own profile; any other is named by its work alone}}).
---

<!--
  Shape and budget: the `project-create-rule-or-skill` skill. Delete any section with nothing real in it, and this
  comment. A skill with several jobs (write, review) gets a Steps section per job, named after it.
-->

# {{Title}}

{{One or two sentences: what this produces, and the one thing that most often makes it go wrong.}}

## Non-negotiables

<!-- The line below is the one `project-create-rule-or-skill`'s *Non-negotiables* allows: keep it word for word in a
     skill whose work writes code or docs, and delete it in any other skill. -->
- **The project's rules and the surrounding file come first — read what `AGENTS.md` loads for this work, if you haven't.**
- **{{An imperative the skill can't run without}}** — {{why, only when it isn't obvious}}

## Before starting

1. Read {{a rule, by name}}.
2. {{A precondition}} — if it doesn't hold, stop and say {{what}}.

## Steps

1. **{{Step}}** — done when {{a checkable outcome}}.

## When the plan stops fitting

- **{{Situation}}** — stop and say {{what}}.

## Report

{{What to tell the user, in order.}}
