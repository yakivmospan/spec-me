<!--
  A module's RELEASING.md — the docs-readme skill's structure. Team-internal: how a release actually
  happens, never shipped to a consumer (that's the README's job) and never named from or copied into
  it. Required: Release. Keep the section order; delete any other section with nothing real under it.
-->

# Releasing

## Release

{{The trigger — a tag pattern, a CI job, a command — and every check it runs before it actually
publishes: version format, ordering, cross-module staleness. What each one rejects, not just that
checks exist.}}

## Custom version

{{Only if a manual override exists — the exact command, and what it does and doesn't bypass.}}
